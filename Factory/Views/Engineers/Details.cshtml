@{
    Layout = "_Layout";
}

@using Factory.Models
@model Factory.Models.Engineer

<h2>Engineer Details</h2>
<hr />

<h3>@Html.DisplayNameFor(model => model.Name) : @Html.DisplayFor(model => model.Name) 
</h3>
@* 
@if(@Model.JoinEntities.Count == 0)
{
    <p>This engineet is currently not assigned to any machines.</p>
}
else
{
    <h4>Machines this engineer is assigned to:</h4>
    <ul>
    @foreach(EngineerMachine join in Model.JoinEntities)
    {
        <li>@join.Machine.Name : @join.Machine.MachineNumber</li>
    }
    </ul>
} *@

<hr />
<p>@Html.ActionLink("Add a Machine", "AddMachine", new {id = Model.EngineerId})
<p>@Html.ActionLink("Back to Engineer list", "index")</p>